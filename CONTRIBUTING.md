# Contributing

## Environment

- Node.js >= 18
- Yarn 4 (Berry)

## Setup

```bash
corepack enable
yarn install --immutable
yarn dlx @yarnpkg/sdks vscode
```

The SDK command is recommended for better TypeScript IntelliSense in VSCode with Yarn Berry.

## Commands

```bash
yarn build
yarn test
```
